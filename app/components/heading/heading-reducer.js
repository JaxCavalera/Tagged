const initialState = {
  // lastUpdated: null,
  // status: null,
  // data: [],
  // filterType: null,//Actions.FILTER_SHOW_ALL,
  // sort: {
  //   column: null,
  //   direction: -1
  // }
};

export default function reducer(state = initialState, action) {
    return state;
}
